import { createContext } from "react";

export const ClickedContext = createContext([false, function () {}]);

export const CharacterContext = createContext([[], function () {}]);

export const EquipmentContext = createContext([[], function () {}]);
